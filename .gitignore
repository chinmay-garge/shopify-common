node_modules/
.DS_Store

# Build output — generated by `npm run build` (esbuild + tailwind).
# Deliberately NOT committed so we can prove CI must build before deploying.
theme/assets/*.vbt.*

# Local Shopify CLI state
.shopify/

# Scratch files produced by local test runs of workflow steps
body.md
cmp.json
v.json
docs/deliverables/
